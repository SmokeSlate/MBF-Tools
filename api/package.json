{
  "name": "mbf-tools-logs-api",
  "private": true,
  "version": "2.0.0",
  "description": "Cloudflare Worker backend for MBF Tools debug log sharing.",
  "type": "module",
  "scripts": {
    "check": "node --check api/handler.js && node --test && wrangler deploy --dry-run --outdir .wrangler-check",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "4.118.0"
  }
}
