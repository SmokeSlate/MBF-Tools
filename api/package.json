{
  "name": "mbf-tools-logs-api",
  "private": true,
  "version": "2.0.0",
  "description": "Cloudflare Worker backend for MBF Tools debug log sharing.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
